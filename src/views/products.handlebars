<main>
  <div class="container">
    <div>
      <h3>{{this.title}}</h3>
      <p>{{this.description}}</p>
      <h3>${{this.price}}</h3>
      <button id="addProduct__btn" value={{product._id}} >Agregar al carrito</button>
    </div>
    <div>
      <img height="50px" width="50px" src={{this.thumbnail}}>
    </div>
  </div>
</main>